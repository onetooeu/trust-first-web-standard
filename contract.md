# OCP Contract

- `/api/v1/` is stable.
- Breaking changes => `/api/v2/`.
- Verify integrity using `/dumps/sha256.json` and signed `/dumps/release.json`.
- No backdoors. Only explicit, signed, auditable changes.


Project operated by **ONETOO.dynamics**.
