# Time capsule

OCP snapshots are designed to be auditable over time.

- Prefer deterministic, signed `dumps/release.json` as the snapshot anchor.
- Old tags should remain available (when feasible).
- Agents may pin to a specific `tag` for reproducibility.


Project operated by **ONETOO.dynamics**.
