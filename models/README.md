# Models / Agents

Optional model/agent cards (machine-readable) for any AI services operated under onetoo.eu.

- `index.json` lists published cards
- each card should be a JSON file with metadata (purpose, limitations, evaluation, contact, etc.)
